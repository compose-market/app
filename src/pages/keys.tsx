/**
 * KeysPage — API key management page.
 *
 * Lists all Inference keys (session + API), allows creating new API keys
 * with custom names/budgets/durations, and revoking keys.
 * No session required — just a connected wallet.
 */

export { ApiKeysPanel as default } from "@/components/keys";
